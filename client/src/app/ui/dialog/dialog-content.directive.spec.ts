import { DialogContentDirective } from './dialog-content.directive';

describe('DialogContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogContentDirective();
    expect(directive).toBeTruthy();
  });
});
